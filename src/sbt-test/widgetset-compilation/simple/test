> compileVaadinWidgetsets

$ exists target/webapp/VAADIN/widgetsets/com.example.MyWidgetset

# gwt-unitCache should not be in the VAADIN dir because it ends up into the generated war file
$ absent target/webapp/VAADIN/gwt-unitCache